version: '3.8'

services:
  telegram-bot:
    build: .
    container_name: ckpool-monitor-bot
    env_file:
      - .env
    volumes:
      - .:/app
      - bot_data:/app/data
    restart: unless-stopped
    command: ruby app.rb
    working_dir: /app

volumes:
  bot_data: